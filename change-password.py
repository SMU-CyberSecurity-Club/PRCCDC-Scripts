# This is the updated change-password.py script

# Remove user confirmation prompts to run automatically

def change_password(username, new_password):
    # Logic for changing the password without user confirmation
    # Assuming we have direct access to necessary credentials
    # Update the password directly in the system
    pass

# Assuming the function will be called elsewhere in the program
